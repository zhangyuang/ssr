service:
  name: serverless-ssr
provider:
  name: aliyun

functions:
  spa:
    handler: spa.handler
    render:
      mode: ssr
    events:
      - http:
          path: /
          method:
            - get

            
package:
  artifact: code.zip

